{
  "name": "moonwalker-trading-bot",
  "version": "1.0.0",
  "description": "Moonwalker Trading Bot — monorepo root",
  "private": true,
  "scripts": {
    "dev": "concurrently -k --kill-signal SIGTERM -n backend,frontend -c cyan,magenta \"npm run dev:backend\" \"wait-on -t 60000 tcp:127.0.0.1:3001 && npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build:frontend": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^8.0.5"
  }
}
